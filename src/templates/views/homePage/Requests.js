import "../styles/Requests.less";
import {Col, Row} from "antd";

const Requests = () => {
  return (
    <Row className="requests--container">
      <Col span={24}>
        Requests
      </Col>
    </Row>
  );
};

export default Requests;
